@layout('layout/app')
@section('content')
<h1>Tasks</h1>

@if(old('notification'))
<div class="alert alert-success" role="alert">
  {{old('notification')}}
</div>
@endif

<div class="list-group">
  @each(item in tasks)
  <a href="/task/{{item.id}}/details" class="list-group-item list-group-item-action">{{item.title}}</a>
  @endeach
</div>
@endsection
